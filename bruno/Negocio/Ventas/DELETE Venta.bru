meta {
  name: DELETE Venta
  type: http
  seq: 6
}

delete {
  url: {{base_url}}/ventas
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "id": 1
  }
}

script:post-response {
  if (res.status === 200) {
    console.log("Venta eliminada exitosamente");
  } else {
    console.log("Error:", res.status, res.body);
  }
}
