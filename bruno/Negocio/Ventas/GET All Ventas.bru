meta {
  name: GET All Ventas
  type: http
  seq: 2
}

get {
  url: {{base_url}}/ventas
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

script:post-response {
  if (res.status === 200) {
    console.log("Ventas obtenidas:", res.body.length);
  } else {
    console.log("Error:", res.status);
  }
}
