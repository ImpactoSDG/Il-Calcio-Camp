meta {
  name: GET All Clientes
  type: http
  seq: 2
}

get {
  url: {{base_url}}/clientes
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

script:post-response {
  if (res.status === 200) {
    console.log("Clientes obtenidos:", res.body.length);
  } else {
    console.log("Error:", res.status);
  }
}
