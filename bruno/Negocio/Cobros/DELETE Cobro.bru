meta {
  name: DELETE Cobro
  type: http
  seq: 7
}

delete {
  url: {{base_url}}/cobros
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "id": 1
  }
}

script:post-response {
  if (res.status === 200) {
    console.log("Cobro eliminado exitosamente");
  } else {
    console.log("Error:", res.status, res.body);
  }
}
