meta {
  name: GET Cobros by Cliente
  type: http
  seq: 3
}

get {
  url: {{base_url}}/cobros?cliente=27123456
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

script:post-response {
  if (res.status === 200) {
    console.log("Cobros del cliente:", res.body.length);
  } else {
    console.log("Error:", res.status);
  }
}
