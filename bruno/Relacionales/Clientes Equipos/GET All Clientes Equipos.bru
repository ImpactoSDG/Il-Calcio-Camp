meta {
  name: GET All Clientes Equipos
  type: http
  seq: 2
}

get {
  url: {{base_url}}/clientes-equipos
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

script:post-response {
  if (res.status === 200) {
    console.log("Relaciones cliente-equipo obtenidas:", res.body.length);
  } else {
    console.log("Error:", res.status);
  }
}
