meta {
  name: DELETE Cliente Equipo
  type: http
  seq: 4
}

delete {
  url: {{base_url}}/clientes-equipos
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "id_cliente_equipo": 1
  }
}

script:post-response {
  if (res.status === 200) {
    console.log("Relación cliente-equipo eliminada exitosamente");
  } else {
    console.log("Error:", res.status, res.body);
  }
}
