meta {
  name: GET All Articulos Venta
  type: http
  seq: 2
}

get {
  url: {{base_url}}/articulos-venta
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

script:post-response {
  if (res.status === 200) {
    console.log("Artículos de venta obtenidos:", res.body.length);
  } else {
    console.log("Error:", res.status);
  }
}
