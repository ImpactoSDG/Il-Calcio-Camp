meta {
  name: GET All Ventas Cobro
  type: http
  seq: 2
}

get {
  url: {{base_url}}/ventas-cobro
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

script:post-response {
  if (res.status === 200) {
    console.log("Relaciones venta-cobro obtenidas:", res.body.length);
  } else {
    console.log("Error:", res.status);
  }
}
