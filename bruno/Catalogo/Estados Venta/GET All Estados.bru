meta {
  name: GET All Estados
  type: http
  seq: 2
}

get {
  url: {{base_url}}/estados-venta
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

script:post-response {
  if (res.status === 200) {
    console.log("Estados obtenidos:", res.body.length);
  } else {
    console.log("Error:", res.status);
  }
}
