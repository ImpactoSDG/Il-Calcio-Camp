meta {
  name: GET All Condiciones IVA
  type: http
  seq: 2
}

get {
  url: {{base_url}}/condiciones-iva
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

script:post-response {
  if (res.status === 200) {
    console.log("Condiciones IVA obtenidas:", res.body.length);
  } else {
    console.log("Error:", res.status);
  }
}
