meta {
  name: GET All Provincias
  type: http
  seq: 2
}

get {
  url: {{base_url}}/provincias
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

script:post-response {
  if (res.status === 200) {
    console.log("Provincias obtenidas:", res.body.length);
  } else {
    console.log("Error:", res.status);
  }
}
