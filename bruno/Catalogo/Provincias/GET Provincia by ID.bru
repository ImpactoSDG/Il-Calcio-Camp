meta {
  name: GET Provincia by ID
  type: http
  seq: 3
}

get {
  url: {{base_url}}/provincias/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

script:post-response {
  if (res.status === 200) {
    console.log("Provincia encontrada:", res.body);
  } else if (res.status === 404) {
    console.log("Provincia no encontrada");
  } else {
    console.log("Error:", res.status);
  }
}
