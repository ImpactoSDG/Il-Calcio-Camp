meta {
  name: GET Medios Cobro Activos
  type: http
  seq: 3
}

get {
  url: {{base_url}}/medios-cobro?activos=1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

script:post-response {
  if (res.status === 200) {
    console.log("Medios de cobro activos:", res.body.length);
  } else {
    console.log("Error:", res.status);
  }
}
