meta {
  name: GET Medio Cobro by ID
  type: http
  seq: 4
}

get {
  url: {{base_url}}/medios-cobro/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

script:post-response {
  if (res.status === 200) {
    console.log("Medio de cobro encontrado:", res.body);
  } else if (res.status === 404) {
    console.log("Medio de cobro no encontrado");
  } else {
    console.log("Error:", res.status);
  }
}
