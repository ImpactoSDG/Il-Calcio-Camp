meta {
  name: GET All Categorias
  type: http
  seq: 2
}

get {
  url: {{base_url}}/categorias-articulo
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

script:post-response {
  if (res.status === 200) {
    console.log("Categorías obtenidas:", res.body.length);
  } else {
    console.log("Error:", res.status);
  }
}
