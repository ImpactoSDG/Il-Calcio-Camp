meta {
  name: GET All Equipos
  type: http
  seq: 2
}

get {
  url: {{base_url}}/equipos
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

script:post-response {
  if (res.status === 200) {
    console.log("Equipos obtenidos:", res.body.length);
  } else {
    console.log("Error:", res.status);
  }
}
