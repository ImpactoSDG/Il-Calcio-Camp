meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{base_url}}/login
  body: json
  auth: none
}

body:json {
  {
    "email": "vfranceschi@impactosdg.com",
    "contrasena": "123456"
  }
}

script:post-response {
  if (res.status === 200) {
    console.log("Login exitoso");
    bru.setEnvVar("token", res.body.token);
  } else {
    console.log("Error en login:", res.status);
  }
}
